class Solution(object):
    def buildArray(self, nums):
        new_nums = [0] * len(nums)
        for index in range(len(nums)):
            new_nums[index] = nums[nums[index]]
        return new_nums
